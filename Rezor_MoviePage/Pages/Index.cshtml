@page
@using Rezor_MoviePage.Components
@using Rezor_MoviePage.Data
@using Rezor_MoviePage.Pages

@model IndexModel

<div class="container" style="display: flex; justify-content: center; min-height: calc(100vh - 80px); ">
    <div class="jumbotron" style="position:center;">
        <h2 class="display-3">Маникюр</h2>
        @(await Html.RenderComponentAsync<Component>(RenderMode.ServerPrerendered))
          <script src="_framework/blazor.service.js"></script>  
        @* <h3 class="display-3">Лист Услуг Мастера:</h3>           =>Через компонент*@
        @* @foreach(var movie in Model.MovieContext.Movies)
        {
            <p style="text-align:center">@movie.Title</p>
        } *@
    </div>
</div>
